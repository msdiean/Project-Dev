namespace: three-tier

replicaCount: 1

image:
  repository: 814495875142.dkr.ecr.us-east-1.amazonaws.com/frontend
  tag: "latest"        # ✅ FIXED (important)
  pullPolicy: Always

imagePullSecrets:
  - name: ecr-registry-secret

service:
  type: ClusterIP
  port: 3000

env:
  backendUrl: "<lb-dns>
